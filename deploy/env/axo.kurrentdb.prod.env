# KurrentDB container configuration (production).
# Single-node, insecure (TLS-off) -- safe only because this container is
# never published to the host, reachable exclusively by axo-vem
# over the internal axo-net network.

EVENTSTORE_INSECURE=true
EVENTSTORE_RUN_PROJECTIONS=All
EVENTSTORE_CLUSTER_SIZE=1
